<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>WinEvo.ActionModel</RootNamespace>
  </PropertyGroup>

  <!-- JsonSchema.Net is re-added when the ManifestLoader JSON-Schema-validation
       TODO is implemented. YamlDotNet is unused (no YAML manifest path exists). -->

</Project>
